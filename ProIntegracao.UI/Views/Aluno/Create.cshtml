@model ProIntegracao.UI.ViewModel.AlunoViewModel
@using (Html.BeginForm("Create", "Aluno", FormMethod.Post, new { @class = "form-horizontal", @id = "formAluno" }))
{
    <div class="row">
        <div class="col-lg-12">
            @{Html.RenderPartial("~/Views/Aluno/_form.cshtml", Model);}
        </div>
    </div>
}
