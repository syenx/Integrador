@model ProIntegracao.UI.ViewModel.MatriculaViewModel
@using (Html.BeginForm("Edit", "Matricula", FormMethod.Post, new { @class = "form-horizontal", @id = "formMatricula" }))
{
    <div class="form-horizontal">
        <div class="row">
            <div class="col-lg-12">
                @{Html.RenderPartial("~/Views/Matricula/_form.cshtml", Model);}
            </div>
        </div>
    </div>
}
