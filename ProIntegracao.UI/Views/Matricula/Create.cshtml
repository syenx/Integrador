@model ProIntegracao.UI.ViewModel.MatriculaViewModel
@section scripts{
    <script src="@Url.Content("~/Scripts/jquery.validate.js")"></script>
    <script src="@Url.Content("~/Scripts/matricula.js")"></script>
}

@using (Html.BeginForm("Create", "Matricula", FormMethod.Post, new { @class = "form-horizontal", @id = "formMatricula" }))
{
    <div class="form-horizontal">
        <div class="row">
            <div class="col-lg-12">
                @{Html.RenderPartial("~/Views/Matricula/_form.cshtml", Model);}
            </div>
        </div>
    </div>
}

