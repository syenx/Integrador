@model ProIntegracao.UI.ViewModel.AulaViewModel
@using (Html.BeginForm("Create", "Aula", FormMethod.Post, new { @class = "form-horizontal", @id = "formAula" }))
{
    <div class="form-horizontal">
        <div class="row">
            <div class="col-lg-12">
                @{Html.RenderPartial("~/Views/Aula/_form.cshtml", Model);}
            </div>
        </div>
    </div>
}
