@model ProIntegracao.UI.ViewModel.PaginaViewModel
@using (Html.BeginForm("Create", "Pagina", FormMethod.Post, new { @class = "form-horizontal", @id = "formPagina" }))
{
    <div class="form-horizontal">
        <div class="row">
            <div class="col-lg-12">
                @{Html.RenderPartial("~/Views/Pagina/_form.cshtml", Model);}
            </div>
        </div>
    </div>
                    }
