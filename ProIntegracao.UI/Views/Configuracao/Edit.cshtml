@model ProIntegracao.UI.ViewModel.ConfiguracaoViewModel
@using (Html.BeginForm("Edit", "Configuracao", FormMethod.Post, new { @class = "form-horizontal", @id = "formConfiguracao" }))
{
    <div class="form-horizontal">
        <div class="row">
            <div class="col-lg-12">
                @{Html.RenderPartial("~/Views/Configuracao/_form.cshtml", Model);}
            </div>
        </div>
    </div>
}
